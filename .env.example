# ==========================================
# RECEHKU - ENVIRONMENT VARIABLES TEMPLATE
# ==========================================
#
# IMPORTANT:
# 1. Copy this file to .env
# 2. Update all values with your actual configuration
# 3. NEVER commit .env to version control
#
# ==========================================

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/recehku_db

# ==========================================
# JWT CONFIGURATION
# ==========================================
# Secret key for JWT token generation (minimum 32 characters)
# Generate using: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-this-to-a-random-secret-key-at-least-32-characters

# JWT token expiration in seconds (86400 = 24 hours)
JWT_ACCESS_TOKEN_EXPIRES=86400

# ==========================================
# FLASK CONFIGURATION
# ==========================================
# Flask secret key for session management (minimum 32 characters)
# Generate using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-another-random-secret-key-at-least-32-characters

# Flask environment (development, production)
FLASK_ENV=development

# Debug mode (true/false) - MUST be false in production
DEBUG=false

# ==========================================
# CORS CONFIGURATION
# ==========================================
# Allowed origins for Cross-Origin Resource Sharing
# Multiple origins separated by comma
# Development:
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Production (replace with your domain):
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================
# Timezone
TIMEZONE=Asia/Jakarta

# Application port
PORT=5000

# ==========================================
# PRODUCTION CHECKLIST
# ==========================================
# Before deploying to production:
# [ ] Generate new random SECRET_KEY and JWT_SECRET_KEY
# [ ] Set FLASK_ENV=production
# [ ] Set DEBUG=false
# [ ] Update CORS_ORIGINS with production domain
# [ ] Use strong database password
# [ ] Enable database SSL/TLS
# [ ] Configure HTTPS
# [ ] Set up firewall rules
# [ ] Configure backup strategy
# [ ] Set up monitoring and logging
# [ ] Review all security settings
